<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ProgressBarSlider.Views.ProgressBarSliderPage"
             xmlns:vm="clr-namespace:ProgressBarSlider.ViewModels">

    
    <ContentPage.BindingContext>
        <vm:ProgressBarSliderViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <ProgressBar x:Name="progressBar"
                         HeightRequest="50"
                         ProgressColor="Orange"
                         VerticalOptions="CenterAndExpand" 
                         Rotation="90"
                         Progress="{Binding SliderProgressValue}"/>

            <Slider Value="{Binding SliderProgressValue}"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>